# PC再生音録音ソフト 仕様書

## 1. 目的

PCで再生中の音声を録音し、WAV または MP3 形式で保存できるシンプルな Windows デスクトップアプリとする。

## 2. 対象OS

- Windows 10
- Windows 11

録音方式は WASAPI Loopback を使用する。

## 3. 基本機能

- 録音開始 / 停止
- 録音対象の再生デバイス選択
- 保存形式選択: WAV / MP3
- MP3 bitrate 選択: 128 / 192 / 320kbps
- 保存先指定
- ファイル名指定または自動生成
- 録音時間表示
- 音量レベル表示
- ステータス表示
- 履歴表示
- ログファイル出力

## 4. 保存仕様

- WAV は録音中に逐次ファイルへ書き込む。
- MP3 も録音中に逐次エンコードしてファイルへ書き込む。
- ファイル名が空の場合は `Recording_YYYYMMDD_HHMMSS` を使用する。
- 選択形式に合わせて拡張子を自動付与する。
- 無効なファイル名文字は `_` に置き換える。
- 同名ファイルが存在する場合は `_001`, `_002` のように自動連番を付ける。
- 保存先が存在しない、書き込めない、空き容量が500MB未満の場合は録音を開始しない。

## 5. 設定

設定は `%AppData%\PcAudioRecorder\settings.json` に保存する。

有効な設定項目:

- `OutputDirectory`
- `OutputFormat`
- `Mp3Bitrate`
- `SelectedRenderDeviceId`

古い設定ファイルに未使用項目が残っていても読み込みに失敗しない。

## 6. ログ

ログは `%AppData%\PcAudioRecorder\logs\app-yyyyMMdd.log` に保存する。

記録対象:

- アプリ起動 / 終了
- 録音開始 / 停止 / 保存完了
- 保存先や録音形式の変更
- 警告
- 例外

## 7. エラー処理

- 録音デバイスが見つからない場合は既定の再生デバイスへフォールバックする。
- 保存先エラーは録音開始前に通知する。
- 録音中の例外はステータス、ダイアログ、ログに表示する。

## 8. 技術構成

- C# + WPF (.NET 8)
- NAudio
- NAudio.Lame
- xUnit
